# AWS App Runner Configuration
version: 1.0

# Runtime configuration
runtime: nodejs22
build:
  commands:
    build:
      - npm install --production
  env:
    - name: NODE_ENV
      value: production

run:
  runtime-version: 22.14.0
  command: npm start
  env:
    - name: NODE_ENV
      value: production
